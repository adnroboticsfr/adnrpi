# Debian 14 (forky, testing) server image. First built experimentally in
# configs-wip/ (single-image workflow, green on the first try 2026-07-27),
# promoted to the main matrix the same day.
BOARD="smartpi1"
BOOTSIZE="512"
BOOTFS_TYPE="fat"           # use FAT filesystem for the /boot directory
RELEASE="forky"
PACKAGE_LIST_BOARD="${PACKAGE_LIST_BOARD} lm-sensors"
