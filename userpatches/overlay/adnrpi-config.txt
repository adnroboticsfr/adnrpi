# ============================================================
# ADNRPi Configuration File
# ============================================================
# Edit this file on the SD card (FAT partition) before first boot.
# Works on Windows, macOS, and Linux — no special software needed.
#
# To apply:
#   1. Edit the values below
#   2. Set APPLY_CONFIG=1
#   3. Save and boot
#
# After first boot this file is renamed to adnrpi-config.txt.done
# ============================================================

# SET TO 1 TO APPLY THIS CONFIGURATION
APPLY_CONFIG=0

# ============ HOSTNAME ============
HOSTNAME=adnrpi

# ============ SSH ============
# 1 = enabled (default), 0 = disabled
SSH_ENABLED=1

# ============ ROOT PASSWORD ============
ROOT_PASSWORD=adnrpi

# ============ USER ============
# User 'pi' is created automatically with sudo access
USERNAME=pi
USER_PASSWORD=adnrpi

# ============ WIFI ============
# Leave empty to skip WiFi configuration
WIFI_SSID=
WIFI_PASSWORD=
WIFI_COUNTRY=FR

# ============ NETWORK ============
# Leave empty for DHCP (automatic IP)
STATIC_IP=
NETMASK=255.255.255.0
GATEWAY=
DNS=8.8.8.8

# ============ TIMEZONE ============
# Examples: Europe/Paris, America/New_York, Asia/Tokyo
TIMEZONE=Europe/Paris

# ============ LOCALE ============
LOCALE=en_US.UTF-8

# ============================================================
# ROS CONFIGURATION (only applies to ROS images)
# ============================================================

# Robot name (used for ROS namespace — leave empty to use HOSTNAME)
ROS_ROBOT_NAME=

# ROS workspace to source automatically on login
# Example: /home/pi/ros_ws
ROS_WORKSPACE=

# ---- ROS1 Noetic ----
# URI of the ROS master node (default: http://localhost:11311)
ROS_MASTER_URI=

# Hostname or IP advertised to the ROS1 network (leave empty = system hostname)
ROS_HOSTNAME=

# IP advertised to the ROS1 network (alternative to ROS_HOSTNAME)
ROS_IP=

# ---- ROS2 Humble ----
# Domain ID for network isolation (0–232, default: 0)
ROS_DOMAIN_ID=

# DDS middleware (leave empty for default cyclonedds)
# Options: rmw_cyclonedds_cpp, rmw_fastrtps_cpp
ROS_RMW_IMPLEMENTATION=
